# Global Rules Folder

Put shared Cursor rules (`*.mdc`) here.

Suggested files:

- `anwar.mdc` (personal/global standards)
- `grabio-pos.mdc` (POS-specific shared rule)

These are synced to `~/.cursor/rules` (or Windows equivalent) by the sync script.
